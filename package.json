{
  "name": "lri-mun-x",
  "private": true,
  "version": "1.0.0",
  "description": "LRI MUN X — the public conference site, the OC operations hub and the API behind both.",
  "workspaces": [
    "apps/backend",
    "apps/hub",
    "apps/site"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:site": "npm run dev --workspace apps/site",
    "dev:hub": "npm run dev --workspace apps/hub",
    "dev:backend": "npm run dev --workspace apps/backend",
    "dev": "concurrently --names api,hub,site --prefix-colors magenta,cyan,yellow --kill-others-on-fail \"npm:dev:backend\" \"npm:dev:hub\" \"npm:dev:site\"",
    "build": "npm run build --workspace apps/backend && npm run build --workspace apps/hub && npm run build --workspace apps/site && node scripts/compose-static.mjs",
    "start": "node apps/backend/dist/index.js",
    "deploy": "prisma generate && prisma migrate deploy && npm run build && node scripts/bootstrap-admin.mjs",
    "typecheck": "npm run typecheck --workspace apps/backend && npm run typecheck --workspace apps/hub",
    "lint": "eslint apps --ext .ts,.tsx",
    "test": "npm run test --workspace apps/backend && npm run test --workspace apps/hub",
    "verify": "npm run lint && npm run typecheck && npm test",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:validate": "prisma validate",
    "prisma:studio": "prisma studio",
    "seed": "tsx prisma/seed.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "//allowScripts": "npm 12 blocks dependency install scripts unless they are listed here. These four need theirs: Prisma downloads and places its query engine, and esbuild places its platform binary. Without this a clean install — which is exactly what a Vercel build does — produces a tree that cannot generate a client or run Vite. Locally you may not notice, because an older node_modules already has the binaries.",
  "allowScripts": {
    "prisma": true,
    "@prisma/client": true,
    "@prisma/engines": true,
    "esbuild": true,
    "core-js": false
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "concurrently": "^10.0.4",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
